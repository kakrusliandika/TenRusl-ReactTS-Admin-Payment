/** @type {import('tailwindcss').Config} */
module.exports = {
  // Di Tailwind v4, daftar file sumber (content scanning) dilakukan otomatis.
  // Kustomisasi utama bisa kamu lakukan di CSS melalui direktif @theme.
  theme: {
    extend: {
      // Contoh tempat kamu nanti menambah warna, font, spacing kustom, dll.
    },
  },
  plugins: [],
}
